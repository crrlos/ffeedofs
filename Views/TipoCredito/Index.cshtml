@model List<TipoCreditoDTO>

<a asp-controller="TipoCredito" asp-action="Register" class="btn btn-primary">Agregar</a>

<table class="table mt-5">
   <thead>
       <tr>
           <th>
               Código
           </th>
           <th>
               Nombre
           </th>
           
           <th>
               Opciones
           </th>
       </tr>
   </thead>
   <tbody>
       @foreach (var cliente in Model)
       {
           <tr>
               <td>@cliente.Codigo</td>
               <td>@cliente.Nombre</td>
               
               <td>
                   @Html.ActionLink("editar","Edit","TipoCredito",new {id = cliente.Id})
                   @Html.ActionLink("eliminar","Delete","TipoCredito",new {id = cliente.Id})
                  
                   </td>
           </tr>
       }
   </tbody>
</table>
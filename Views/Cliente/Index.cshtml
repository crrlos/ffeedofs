@model List<ClienteDTO>

<a asp-controller="Cliente" asp-action="Register" class="btn btn-primary">Agregar</a>

<table class="table mt-5">
   <thead>
       <tr>
           <th>
               Código
           </th>
           <th>
               Nombre
           </th>
           <th>
               Apellidos
           </th>
           <th>
               Opciones
           </th>
       </tr>
   </thead>
   <tbody>
       @foreach (var cliente in Model)
       {
           <tr>
               <td>@cliente.Codigo</td>
               <td>@cliente.Nombre</td>
               <td>@cliente.Apellidos</td>
               <td>
                   @Html.ActionLink("editar","Edit","Cliente",new {id = cliente.Id})
                   @Html.ActionLink("eliminar","Delete","Cliente",new {id = cliente.Id})
                  
                   </td>
           </tr>
       }
   </tbody>
</table>